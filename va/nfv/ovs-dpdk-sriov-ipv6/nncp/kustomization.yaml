---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# This component overrides the NNCP configuration to use IPv6 addressing
# It uses the lib/nncp_ipv6 component instead of lib/nncp

components:
  - ../../../../lib/nncp_ipv6
